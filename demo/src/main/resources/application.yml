spring:
  application:
    name: demo
  ai:
    mcp:
      server:
        name: enterprise-order-mcp-system
        version: 1.0.0
        # 指定使用流式HTTP无状态协议
        protocol: STREAMABLE
  server:
    port: 8080

agentscope:
  core:
    model:
      dashscope:
        api-key: ${DASHSCOPE_API_KEY}  # ???????
        model-name: qwen-plus-latest   # ????Plus??
    agent:
      max-steps: 10                    # ??????